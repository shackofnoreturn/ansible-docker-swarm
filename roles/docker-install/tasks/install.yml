---
- name: Install Docker
  ansible.builtin.apt:
    name: "{{ docker_packages }}"
    state: latest
    update_cache: true

- name: Enable Docker
  ansible.builtin.service:
    name: docker
    enabled: "{{ docker_service_enabled }}"
    state: "{{ docker_service_state }}"
